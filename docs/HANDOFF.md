# PolitiTrack active handoff

## September 22 independent manual OCR and discovery evidence (#225) — source verification

Canonical repository ID 1349678672, `maglothinm/MyETF-Intelligence`.
Branch `codex/manual-ocr-discovery-evidence`, based on main
`aaf7e97c48e1afe5efbdbf3c966f3f9e72f90a08`.

Executive manual uploads now have a separate maintenance commit before fresh
collection, under the same existing namespace lock. OGE failures remain collector
failures and do not erase a committed extraction. No new scheduler/writer,
automatic-source exemption, state reset or review bypass is introduced.

AI/Current Opportunity adds persisted, factual information_value_at_discovery
evidence and JSON/CSV/dashboard reporting. The first usable quote is retained per
transaction. Missing required evidence is unknown; legacy prices are retained
without inventing quote quality or adjustment provenance. See DISCOVERY_EVIDENCE.md.
The preserved legacy analyst is unchanged.

Local regression: 547 passed, 7 PostgreSQL cases skipped; 10 interface/accessibility
tests passed. Final focused check: 48 passed, 1 PostgreSQL test skipped. New real
PostgreSQL acceptance covers maintenance replay, atomic receipts, and independent
collector health in canonical CI. Exact-head CI, merge, activation and live proof
remain outstanding at this source checkpoint.

Read-only live audit at this session found application/config dfb5c66 and newly
started jobs using it, superseding the earlier canceled-activation statement.
The 30-page upload remains pending, with no snapshot receipt. Its immutable source
identity and raw file remain in the private inbox; do not upload it again.
Executive generation 692 remains its last successful source head at this checkpoint.
Old source and AI heads are historical evidence, not a reason to rebaseline.

Next safe action: complete exact-head CI, merge only tested source, then use normal
Beast service controls for a safe activation. Preserve the database service,
immutable snapshot history, local configuration and all untracked files. Verify
the existing upload reaches committed extraction/review during an OGE failure,
and the normal AI/dashboard producers publish discovery evidence. Import remains
subject to existing owner review. Cloud and legacy producers remain retired.
